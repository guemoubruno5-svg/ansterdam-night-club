/* eslint-disable react-hooks/set-state-in-effect */
import { AnimatePresence, motion } from 'framer-motion'
import { Menu, X } from 'lucide-react'
import { useEffect, useState } from 'react'
import { Link, NavLink, useLocation } from 'react-router-dom'
import { ClubLogo } from '../shared/ClubLogo'
const nav = [['/', 'Accueil'], ['/events', 'Événements'], ['/club', 'Notre Club'], ['/rooms', 'Chambres'], ['/gallery', 'Galerie'], ['/contact', 'Contact']]
export function Header() { const [open, setOpen] = useState(false); const location = useLocation(); useEffect(() => setOpen(false), [location]); return <header className="site-header"><Link className="header-logo" to="/" aria-label="ANSTERDAM NIGHT CLUB — Accueil"><ClubLogo size="small" /></Link><nav className="desktop-nav" aria-label="Navigation principale">{nav.map(([to, label]) => <NavLink key={to} to={to}>{label}</NavLink>)}<Link className="btn btn-primary compact" to="/reservation">RÉSERVER</Link></nav><button className="menu-toggle" onClick={() => setOpen(!open)} aria-expanded={open} aria-label={open ? 'Fermer le menu' : 'Ouvrir le menu'}>{open ? <X /> : <Menu />}</button><AnimatePresence>{open && <motion.nav className="mobile-nav" initial={{ opacity: 0, y: -12 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -12 }}><Link className="mobile-nav-logo" to="/" aria-label="ANSTERDAM NIGHT CLUB — Accueil"><ClubLogo size="small" showName /></Link>{nav.map(([to, label]) => <NavLink key={to} to={to}>{label}</NavLink>)}<Link className="btn btn-primary" to="/reservation">RÉSERVER</Link></motion.nav>}</AnimatePresence></header> }
